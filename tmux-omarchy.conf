# Tell tpm where plugins live (they're in ~/.tmux/plugins/, not ~/.config/tmux/plugins/)
set-environment -g TMUX_PLUGIN_MANAGER_PATH "~/.tmux/plugins/"

# Personal config (plugins, bindings, theme) — omarchy never touches this file
source-file ~/.tmux.conf
